@model IEnumerable<OnlineShop4DVDS.Models.Genre>

@{
    ViewData["Title"] = "GenreView";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<h1>GenreView</h1>

<p>
    <a asp-action="GenreInsert">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GenreName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GenreName)
            </td>
            <td>
                <a asp-action="GenreUpdate" asp-route-id="@item.GenreId">Edit</a> |
                <form method="post" asp-action="GenreDelete" style="display: inline;">
                    <input type="hidden" name="id" value="@item.GenreId"/>
                        <button type="submit" onclick="return confirm('Are you sure you want to delete this developer?');" class="btn btn-danger">Delete</button>
                    </form>
            </td>
        </tr>
}
    </tbody>
</table>
