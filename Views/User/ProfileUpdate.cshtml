@model OnlineShop4DVDS.Models.User

@{
    ViewData["Title"] = "ProfileUpdate";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Normal Breadcrumb Begin -->
<section class="normal-breadcrumb set-bg" data-setbg="img/normal-breadcrumb.jpg">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <div class="normal__breadcrumb__text">
                    @if (!string.IsNullOrEmpty(ViewBag.UserName))
                    {
                        <h2>Hello, @ViewBag.UserName!</h2>
                    }
                    else
                    {
                        <h2>Welcome, Guest!</h2>
                    }
                </div>
            </div>
        </div>
    </div>
</section>
<!-- Normal Breadcrumb End -->
<!-- Signup Section Begin -->
<section class="signup spad">
    <div class="container">
        <div class="row">
            <div class="col-lg-6">
                <div class="login__form">
                    <h3>Update Profile</h3>
                    <form asp-action="ProfileUpdate" method="post">
                        <span asp-validation-for="UserEmail" class="text-danger"></span>
                        <div class="input__item">
                            <input asp-for="UserEmail" type="text" placeholder="Email address">
                        </div>
                        <span asp-validation-for="UserName" class="text-danger"></span>
                        <div class="input__item">
                            <input asp-for="UserName" type="text" placeholder="Your Name">
                        </div>
                        <button type="submit" class="site-btn">Update Profile</button>
                        <form asp-action="DeleteAccount" method="post">
                            <button type="submit" class="site-btn" onclick="return confirm('Are you sure you want to delete your account? This action is irreversible!');">Delete Account</button>
                        </form>
                    </form>
                </div>
            </div>
            <div class="col-lg-6">
                <div class="login__social__links">
                    <h3>Order History:</h3>
                    @if (ViewBag.Orders != null)
                    {
                        <table class="table table-bordered">
                            <thead class="thead-light">
                                <tr>
                                    <th>Order ID</th>
                                    <th>Order Date</th>
                                    <th>Total Amount</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var order in ViewBag.Orders)
                                {
                                    <tr class="text-white">
                                        <td>@order.OrderId</td>
                                        <td>@order.OrderDate.ToString("yyyy-MM-dd")</td>
                                        <td>$@order.TotalAmount</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <p>No orders found.</p>
                    }
                </div>
            </div>
        </div>
    </div>
</section>
<!-- Signup Section End -->

@* <h1>ProfileUpdate</h1>

<h4>User</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ProfileUpdate" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserName" class="control-label"></label>
                <input asp-for="UserName" class="form-control" />
                <span asp-validation-for="UserName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="UserEmail" class="control-label"></label>
                <input asp-for="UserEmail" class="form-control" />
                <span asp-validation-for="UserEmail" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Update" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div> *@

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
